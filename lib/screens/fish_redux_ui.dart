import 'package:flutter/material.dart';

class FishReduxExample extends StatelessWidget {
  const FishReduxExample({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
